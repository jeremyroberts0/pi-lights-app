import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    action: {
      fontSize: 18
    },
    card: {
      margin: 10
    },
    center: {
      textAlign: 'center'
    }
  });

export default styles
